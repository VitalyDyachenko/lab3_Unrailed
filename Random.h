// Random.h:
#pragma once

#include <random>

using namespace std;

// Функция для случайного double в диапазоне [0, 1)
double randomDouble();
// Функция для случайного int в диапазоне [min, max]
int randomInt(int min, int max);